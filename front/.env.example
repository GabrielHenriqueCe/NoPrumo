# Copy to .env — the root .gitignore keeps .env out of the repository.
# Nothing secret belongs here: an address is not a credential.

# Where the .NET API answers
# (see back/01-Presentation/Properties/launchSettings.json)
VITE_API_URL=http://localhost:5262/api
